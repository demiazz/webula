<%= form_tag(profile_master__avatar_save_path, :multipart => true, :method => "put") do %>
	<div class="field">
		<%= label_tag(:avatar, "Avatar file:") %><br />
		<%= file_field_tag(:avatar) %>
	</div>
	<div class="actions">
    	<%= submit_tag("Finish") %>
  	</div>
<% end %>